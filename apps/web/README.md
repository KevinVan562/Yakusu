# Yakusu Web

React + Vite frontend for Yakusu.

## Setup

```bash
npm install
npm run dev
```

The app uses `VITE_API_BASE_URL` to call the API.

For local development:

```bash
cp .env.example .env
```

The default local file points at:

```text
http://127.0.0.1:8000
```
