{
  "name": "yakusu",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "setup:web": "npm install --prefix apps/web",
    "setup:api": "python3 -m venv services/api/.venv && services/api/.venv/bin/pip install -r services/api/requirements.txt",
    "setup": "npm run setup:web && npm run setup:api",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:api": "cd services/api && .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build:web": "npm run build --prefix apps/web"
  }
}
